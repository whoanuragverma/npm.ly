const{exec:exec}=require("child_process"),latestVersion=name=>{const command="npm view "+name+" version";return new Promise((resolve,reject)=>{exec(command,(err,stdout,stderr)=>{err?reject(err):resolve(stdout)})})};